location /uploads/ {
    root          /home/box/web/uploads;
}
location ~* ^.+\.(jpg|jpeg|gif)$ {
    root          /home/box/web/public;
    access_log    off;
    expires       30d;
}
location ~* ^.+\.$ {
    return        404;
}
